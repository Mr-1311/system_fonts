name: system_fonts
description: "A Flutter package to load system fonts on Windows, MacOS, and Linux platforms."
version: 1.0.0
homepage: "https://github.com/Mr-1311/system_fonts"

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: